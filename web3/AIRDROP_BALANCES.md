# Балансы контракта Airdrop

## 💰 Что должно быть в контракте

### ✅ 1. Баланс Savitri токенов — ДА, ОБЯЗАТЕЛЬНО

**Контракт должен иметь баланс токенов для отправки получателям.**

**Как пополнить:**
```javascript
// Owner переводит токены в контракт
await savitriCoin.transfer(airdropAddress, totalAmount);

// Проверить баланс
const balance = await airdrop.getBalance();
console.log("Balance:", ethers.utils.formatEther(balance));
```

**Пример:**
```
Если у вас 1000 получателей по 1000 токенов каждый:
Total = 1,000,000 токенов

Переводите в контракт:
await savitriCoin.transfer(airdropAddress, ethers.utils.parseEther("1000000"));
```

---

### ❌ 2. Баланс BNB для газа — НЕТ, НЕ НУЖЕН

**Контракт НЕ должен иметь BNB для газа.**

**Почему:**
- Газ платит тот, кто вызывает функцию (owner)
- Owner вызывает `batchSend()` или `batchSendDirect()`
- Owner платит газ из своего кошелька
- Контракт только отправляет токены получателям

**Важно:**
- ✅ Owner должен иметь BNB в своем кошельке (для оплаты газа)
- ❌ Контракт НЕ должен иметь BNB

---

## 📊 Структура балансов

### Контракт Airdrop:

```
Баланс контракта:
├── Savitri токены: 1,000,000 токенов ✅ (нужно)
└── BNB: 0 BNB ❌ (не нужно)
```

### Owner кошелек (Gnosis Safe):

```
Баланс owner:
├── Savitri токены: 0 (уже переведены в контракт)
└── BNB: 0.1 BNB ✅ (нужно для оплаты газа)
```

---

## 🔧 Процесс работы

### Шаг 1: Подготовка

**Owner переводит токены в контракт:**
```javascript
// 1. Проверяем баланс owner
const ownerBalance = await savitriCoin.balanceOf(ownerAddress);
console.log("Owner balance:", ethers.utils.formatEther(ownerBalance));

// 2. Переводим токены в контракт
const totalAmount = ethers.utils.parseEther("1000000"); // 1M токенов
await savitriCoin.transfer(airdropAddress, totalAmount);

// 3. Проверяем баланс контракта
const contractBalance = await airdrop.getBalance();
console.log("Contract balance:", ethers.utils.formatEther(contractBalance));
```

**Результат:**
```
Owner balance: 0 токенов
Contract balance: 1,000,000 токенов ✅
```

---

### Шаг 2: Отправка токенов

**Owner вызывает функцию (платит газ):**
```javascript
// Owner платит газ из своего кошелька
await airdrop.batchSendDirect(
    recipients,
    amounts
);
// Gas: ~0.001 BNB (примерно)
```

**Что происходит:**
1. Owner платит газ (из своего кошелька)
2. Контракт отправляет токены получателям (из баланса контракта)
3. Баланс контракта уменьшается

**После отправки:**
```
Contract balance: 900,000 токенов (отправили 100,000)
Owner BNB: 0.099 BNB (потратили 0.001 на газ)
```

---

### Шаг 3: Проверка балансов

**Проверить баланс контракта:**
```javascript
const balance = await airdrop.getBalance();
console.log("Remaining tokens:", ethers.utils.formatEther(balance));
```

**Проверить, сколько отправлено:**
```javascript
// Считаем вручную или через события
// Или просто: начальный баланс - текущий баланс
const initialBalance = ethers.utils.parseEther("1000000");
const currentBalance = await airdrop.getBalance();
const sent = initialBalance.sub(currentBalance);
console.log("Sent:", ethers.utils.formatEther(sent));
```

---

## 💡 Важные моменты

### 1. Контракт должен иметь достаточно токенов

**Проблема:**
```javascript
// Контракт имеет 1000 токенов
// Пытаетесь отправить 2000 токенов
await airdrop.batchSendDirect(
    ["0x1234...", "0x5678..."],
    [
        ethers.utils.parseEther("1000"),
        ethers.utils.parseEther("1000")
    ]
);
// ❌ Ошибка: "Token transfer failed" (недостаточно токенов)
```

**Решение:**
```javascript
// Перевести больше токенов в контракт
await savitriCoin.transfer(airdropAddress, ethers.utils.parseEther("2000"));
```

---

### 2. Owner должен иметь BNB для газа

**Проблема:**
```javascript
// Owner имеет 0 BNB
await airdrop.batchSendDirect(recipients, amounts);
// ❌ Ошибка: "insufficient funds for gas"
```

**Решение:**
```javascript
// Пополнить баланс owner (Gnosis Safe)
// Через биржу или другой кошелек
// Нужно ~0.01-0.1 BNB для нескольких транзакций
```

---

### 3. Можно вывести оставшиеся токены

**Если остались неотправленные токены:**
```javascript
const remaining = await airdrop.getBalance();
if (remaining > 0) {
    await airdrop.withdrawTokens(ownerAddress, remaining);
}
```

---

## 📝 Пример полного процесса

### 1. Начальное состояние

```
Owner (Gnosis Safe):
├── Savitri: 1,000,000 токенов
└── BNB: 0.1 BNB

Airdrop контракт:
├── Savitri: 0 токенов
└── BNB: 0 BNB
```

### 2. Перевод токенов в контракт

```javascript
await savitriCoin.transfer(airdropAddress, ethers.utils.parseEther("1000000"));
```

```
Owner:
├── Savitri: 0 токенов (переведены)
└── BNB: 0.1 BNB

Airdrop контракт:
├── Savitri: 1,000,000 токенов ✅
└── BNB: 0 BNB
```

### 3. Отправка токенов (батч 1)

```javascript
await airdrop.batchSendDirect(recipients1, amounts1);
// Gas: 0.001 BNB
```

```
Owner:
├── Savitri: 0 токенов
└── BNB: 0.099 BNB (потратили 0.001)

Airdrop контракт:
├── Savitri: 900,000 токенов (отправили 100,000)
└── BNB: 0 BNB
```

### 4. Отправка токенов (батч 2)

```javascript
await airdrop.batchSendDirect(recipients2, amounts2);
// Gas: 0.001 BNB
```

```
Owner:
├── Savitri: 0 токенов
└── BNB: 0.098 BNB (потратили еще 0.001)

Airdrop контракт:
├── Savitri: 800,000 токенов (отправили еще 100,000)
└── BNB: 0 BNB
```

### 5. Вывод оставшихся токенов

```javascript
const remaining = await airdrop.getBalance(); // 800,000
await airdrop.withdrawTokens(ownerAddress, remaining);
```

```
Owner:
├── Savitri: 800,000 токенов (вернули)
└── BNB: 0.097 BNB

Airdrop контракт:
├── Savitri: 0 токенов
└── BNB: 0 BNB
```

---

## 🔒 Безопасность

### Проверки перед отправкой:

**1. Проверить баланс контракта:**
```javascript
const balance = await airdrop.getBalance();
const totalToSend = amounts.reduce((sum, amt) => sum.add(amt), ethers.BigNumber.from(0));

if (balance.lt(totalToSend)) {
    throw new Error("Not enough tokens in contract!");
}
```

**2. Проверить баланс owner (для газа):**
```javascript
const ownerBNB = await ethers.provider.getBalance(ownerAddress);
const minBNB = ethers.utils.parseEther("0.01"); // Минимум для транзакций

if (ownerBNB.lt(minBNB)) {
    throw new Error("Not enough BNB for gas!");
}
```

---

## 📊 Резюме

### Контракт Airdrop должен иметь:

| Токен | Нужно? | Зачем? |
|-------|--------|--------|
| **Savitri токены** | ✅ ДА | Для отправки получателям |
| **BNB** | ❌ НЕТ | Газ платит owner |

### Owner (Gnosis Safe) должен иметь:

| Токен | Нужно? | Зачем? |
|-------|--------|--------|
| **Savitri токены** | ✅ ДА (изначально) | Перевести в контракт |
| **BNB** | ✅ ДА | Для оплаты газа при вызове функций |

### Процесс:

1. ✅ Owner переводит Savitri токены в контракт
2. ✅ Owner имеет BNB для оплаты газа
3. ✅ Owner вызывает `batchSend()` или `batchSendDirect()`
4. ✅ Контракт отправляет токены из своего баланса
5. ✅ Owner платит газ из своего кошелька

**Вывод:**
- Контракт = хранилище токенов (только Savitri)
- Owner = плательщик газа (BNB в своем кошельке)

