# Куда попадают токены при создании контракта?

## 📍 Ответ

**Токены попадают на баланс кошелька, который опубликовал (задеплоил) смарт-контракт**, а НЕ на баланс самого контракта.

## 🔍 Как это работает

Посмотрим на код `SavitriCoin.sol`:

```solidity
constructor() ERC20("Savitri Coin", "SAV") {
    _mint(msg.sender, INITIAL_SUPPLY * 10 ** decimals());
    allowedSenders[msg.sender] = true; // Owner can distribute tokens
}
```

### Что происходит:

1. **`msg.sender`** - это адрес кошелька, который вызвал конструктор (т.е. задеплоил контракт)
2. **`_mint(msg.sender, amount)`** - создает токены и отправляет их на адрес `msg.sender`
3. **Результат**: Все 600,000,000 токенов попадают на баланс вашего кошелька

## 📊 Пример

### При деплое:

```javascript
// Вы деплоите контракт с вашего кошелька
const deployer = "0xYourWalletAddress";
const savitriToken = await SavitriCoin.deploy();

// После деплоя:
const deployerBalance = await savitriToken.balanceOf(deployer);
// deployerBalance = 600,000,000 * 10^18 (все токены!)

const contractBalance = await savitriToken.balanceOf(savitriToken.address);
// contractBalance = 0 (контракт пустой!)
```

## 🎯 Почему так?

### Преимущества:
- ✅ **Контроль**: Вы сразу получаете все токены и можете управлять ими
- ✅ **Гибкость**: Можете распределять токены как нужно (ICO, аирдроп, команде)
- ✅ **Безопасность**: Токены не застрянут в контракте

### Что делать дальше:

1. **Для ICO**: Перевести токены в контракт TokenICO
   ```javascript
   await savitriToken.transfer(tokenICO.address, ethers.utils.parseEther("500000000"));
   ```

2. **Для аирдропа**: Перевести токены в контракт Airdrop
   ```javascript
   await savitriToken.transfer(airdrop.address, ethers.utils.parseEther("50000000"));
   ```

3. **Для команды**: Перевести токены на кошельки команды
   ```javascript
   await savitriToken.transfer(teamWallet, ethers.utils.parseEther("10000000"));
   ```

## ⚠️ Важно!

### Если деплоите через Gnosis Safe:

Если вы деплоите контракт через Gnosis Safe (как owner), то:

```javascript
// Деплой через Safe
const safeAddress = "0xSafeAddress";
// msg.sender = safeAddress (адрес Safe)

// После деплоя:
const safeBalance = await savitriToken.balanceOf(safeAddress);
// safeBalance = 600,000,000 * 10^18 (все токены в Safe!)

// Теперь для управления токенами нужна мультиподпись через Safe
```

**Рекомендация**: Деплойте через Gnosis Safe, чтобы все токены сразу были под защитой мультиподписи.

## 📝 Сравнение

| Вариант | Куда попадают токены | Кто контролирует |
|---------|---------------------|------------------|
| **Деплой с обычного кошелька** | На ваш кошелек | Вы (один ключ) |
| **Деплой через Gnosis Safe** | На адрес Safe | Safe (мультиподпись) |

## 🔐 Безопасность

### ✅ Рекомендуется:
1. Деплоить через Gnosis Safe
2. Токены сразу под защитой мультиподписи
3. Для распределения нужны несколько подписей

### ❌ Не рекомендуется:
1. Деплоить с обычного кошелька
2. Хранить все токены на одном кошельке
3. Использовать один приватный ключ

## 💡 Итог

**При создании контракта:**
- ✅ Токены попадают на баланс **кошелька, который задеплоил контракт** (`msg.sender`)
- ❌ Токены **НЕ** попадают на баланс самого контракта
- ✅ Контракт изначально имеет баланс **0 токенов**

**После деплоя вы можете:**
- Перевести токены в ICO контракт
- Перевести токены в Airdrop контракт
- Распределить токены команде
- Оставить часть токенов в Safe для будущего использования

---

**Пример кода:**
```solidity
// В конструкторе:
_mint(msg.sender, INITIAL_SUPPLY * 10 ** decimals());
// msg.sender = адрес, который задеплоил контракт
// Все токены минтируются на этот адрес
```

