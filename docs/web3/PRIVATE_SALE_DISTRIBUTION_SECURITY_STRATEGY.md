# Стратегия безопасности для Airdrop контракта

## ✅ Ваше понимание правильное!

### 1. Если owner взломают → можно создать новый контракт

**Да, это безопасно:**
- Airdrop контракт — это отдельный контракт
- Если owner взломают, можно задеплоить новый Airdrop контракт
- На монету (SavitriCoin) это **не повлияет**

**Процесс:**
```
1. Взломан owner (Gnosis Safe)
   → Злоумышленник может вывести токены из Airdrop контракта

2. Что делать:
   ✅ Создать новый Airdrop контракт
   ✅ Перевести оставшиеся токены в новый контракт
   ✅ Продолжить аирдроп

3. SavitriCoin:
   ✅ Не пострадает (это отдельный контракт)
   ✅ Токены останутся в безопасности
```

---

### 2. Пополнять постепенно — правильная стратегия

**Не скидывать все токены сразу:**
- ✅ Переводить токены порциями (например, по 100,000 токенов)
- ✅ Отправлять батчами
- ✅ Пополнять по мере необходимости

**Преимущества:**
- ✅ Ограничивает потери при взломе
- ✅ Можно контролировать процесс
- ✅ Легче отслеживать баланс

---

## 🔒 Стратегия безопасности

### Вариант 1: Постепенное пополнение (рекомендуется)

**Процесс:**
```
1. Начало: Перевести 100,000 токенов в Airdrop контракт
2. Отправить батчи получателям
3. Когда останется 10,000 токенов → пополнить еще 100,000
4. Повторять до завершения аирдропа
```

**Преимущества:**
- ✅ Максимальный риск = 100,000 токенов (если взломают)
- ✅ Контроль процесса
- ✅ Легко отслеживать

**Пример:**
```javascript
// Начало: 100,000 токенов
await savitriCoin.transfer(airdropAddress, ethers.utils.parseEther("100000"));

// Отправляем батчи...
await airdrop.batchSendDirect(recipients1, amounts1);
await airdrop.batchSendDirect(recipients2, amounts2);

// Когда осталось 10,000 → пополняем
const remaining = await airdrop.getBalance();
if (remaining.lt(ethers.utils.parseEther("10000"))) {
    await savitriCoin.transfer(airdropAddress, ethers.utils.parseEther("100000"));
}
```

---

### Вариант 2: Лимит на вывод (дополнительная защита)

**Можно добавить в контракт:**
```solidity
uint256 public maxWithdrawAmount = 100_000 * 1e18; // Максимум за раз

function withdrawTokens(address to, uint256 amount) external onlyOwner {
    require(amount <= maxWithdrawAmount, "Amount too large");
    // ...
}
```

**Преимущества:**
- ✅ Даже если взломают, нельзя вывести все сразу
- ✅ Ограничивает потери

**Недостатки:**
- ⚠️ Нужно изменять контракт
- ⚠️ Может быть неудобно для больших выводов

---

### Вариант 3: Мультиподпись для вывода (максимальная защита)

**Использовать Gnosis Safe для owner:**
- ✅ Owner = Gnosis Safe (3 из 5 подписей)
- ✅ Для вывода токенов нужны 3 подписи
- ✅ Даже если взломают 1-2 кошелька, нельзя вывести

**Уже реализовано:**
- Owner должен быть Gnosis Safe (рекомендуется)
- Это уже защита от взлома одного кошелька

---

## 📊 Сравнение стратегий

| Стратегия | Максимальный риск | Удобство | Рекомендация |
|-----------|-------------------|----------|--------------|
| **Все токены сразу** | 100% токенов | ✅ Просто | ❌ Небезопасно |
| **Постепенное пополнение** | ~10-20% токенов | ✅ Удобно | ✅ Рекомендуется |
| **Лимит на вывод** | ~10-20% токенов | ⚠️ Средне | ✅ Дополнительно |
| **Gnosis Safe** | Минимальный | ✅ Удобно | ✅ Обязательно |

---

## 🎯 Рекомендуемая стратегия

### Комбинация подходов:

**1. Gnosis Safe для owner (обязательно):**
```javascript
// Owner = Gnosis Safe (3 из 5 подписей)
const airdrop = await Airdrop.deploy(tokenAddress);
// Owner автоматически = Gnosis Safe адрес
```

**2. Постепенное пополнение:**
```javascript
// Пополнять порциями по 100,000 токенов
const BATCH_AMOUNT = ethers.utils.parseEther("100000");
const MIN_BALANCE = ethers.utils.parseEther("10000");

// Проверка и пополнение
async function checkAndRefill() {
    const balance = await airdrop.getBalance();
    if (balance.lt(MIN_BALANCE)) {
        console.log("Refilling airdrop contract...");
        await savitriCoin.transfer(airdropAddress, BATCH_AMOUNT);
    }
}
```

**3. Мониторинг баланса:**
```javascript
// Регулярно проверять баланс
const balance = await airdrop.getBalance();
console.log("Airdrop balance:", ethers.utils.formatEther(balance));

// Если баланс подозрительно уменьшился → проверить транзакции
```

---

## 🔍 Что делать при взломе

### Если owner взломали:

**1. Немедленные действия:**
```javascript
// Проверить баланс контракта
const balance = await airdrop.getBalance();
console.log("Remaining tokens:", ethers.utils.formatEther(balance));

// Проверить последние транзакции
// Если есть подозрительные выводы → действовать быстро
```

**2. Создать новый контракт:**
```javascript
// Деплой нового Airdrop контракта
const newAirdrop = await Airdrop.deploy(tokenAddress);

// Вывести оставшиеся токены из старого контракта (если возможно)
// Или просто продолжить с новым контрактом
```

**3. Продолжить аирдроп:**
```javascript
// Перевести токены в новый контракт
await savitriCoin.transfer(newAirdropAddress, remainingAmount);

// Продолжить отправку
await newAirdrop.batchSendDirect(remainingRecipients, remainingAmounts);
```

**4. SavitriCoin не пострадает:**
- ✅ SavitriCoin — отдельный контракт
- ✅ Токены в безопасности
- ✅ Можно продолжить работу

---

## 📝 Практический пример

### Начальная настройка:

```javascript
// 1. Деплой контракта (owner = Gnosis Safe)
const airdrop = await Airdrop.deploy(tokenAddress);

// 2. Пополнение первой порцией
const FIRST_BATCH = ethers.utils.parseEther("100000");
await savitriCoin.transfer(airdropAddress, FIRST_BATCH);

console.log("Initial balance:", ethers.utils.formatEther(await airdrop.getBalance()));
// Output: Initial balance: 100000.0
```

### Процесс отправки:

```javascript
// 3. Отправка батчей
const recipients = [...]; // 1000 получателей
const amounts = [...];    // Суммы

// Отправляем по 100 адресов за раз
for (let i = 0; i < recipients.length; i += 100) {
    const batch = recipients.slice(i, i + 100);
    const batchAmounts = amounts.slice(i, i + 100);
    
    await airdrop.batchSendDirect(batch, batchAmounts);
    
    // Проверяем баланс
    const balance = await airdrop.getBalance();
    console.log(`After batch ${i/100 + 1}: ${ethers.utils.formatEther(balance)} tokens`);
    
    // Если осталось мало → пополняем
    if (balance.lt(ethers.utils.parseEther("10000"))) {
        console.log("Refilling...");
        await savitriCoin.transfer(airdropAddress, FIRST_BATCH);
    }
}
```

### Мониторинг:

```javascript
// Регулярная проверка
async function monitorAirdrop() {
    const balance = await airdrop.getBalance();
    const expectedBalance = calculateExpectedBalance(); // Ваша логика
    
    if (balance.lt(expectedBalance)) {
        console.warn("⚠️ Balance lower than expected!");
        console.warn("Check recent transactions!");
    }
}
```

---

## 🔒 Дополнительные меры безопасности

### 1. Алерты на подозрительную активность

```javascript
// Настроить мониторинг событий
airdrop.on("TokensWithdrawn", (to, amount) => {
    console.warn("⚠️ Tokens withdrawn:", {
        to,
        amount: ethers.utils.formatEther(amount)
    });
    
    // Отправить уведомление (email, Telegram, etc.)
});
```

### 2. Лимит на одну транзакцию вывода

```javascript
// В контракте можно добавить:
uint256 public constant MAX_SINGLE_WITHDRAW = 50_000 * 1e18;

function withdrawTokens(address to, uint256 amount) external onlyOwner {
    require(amount <= MAX_SINGLE_WITHDRAW, "Amount too large");
    // ...
}
```

### 3. Таймлок для вывода (опционально)

```javascript
// Можно добавить задержку перед выводом
uint256 public withdrawDelay = 24 hours;
mapping(address => uint256) public withdrawRequestTime;

function requestWithdraw(uint256 amount) external onlyOwner {
    withdrawRequestTime[msg.sender] = block.timestamp;
}

function executeWithdraw(address to, uint256 amount) external onlyOwner {
    require(
        block.timestamp >= withdrawRequestTime[msg.sender] + withdrawDelay,
        "Withdraw delay not passed"
    );
    // ...
}
```

---

## 📊 Резюме

### ✅ Ваше понимание правильное:

1. **Если owner взломают:**
   - ✅ Можно создать новый Airdrop контракт
   - ✅ SavitriCoin не пострадает
   - ✅ Токены в безопасности

2. **Пополнять постепенно:**
   - ✅ Не скидывать все токены сразу
   - ✅ Пополнять порциями (100,000 токенов)
   - ✅ Ограничивает потери при взломе

3. **Рекомендации:**
   - ✅ Использовать Gnosis Safe для owner
   - ✅ Пополнять постепенно
   - ✅ Мониторить баланс
   - ✅ Иметь план на случай взлома

### 🎯 Итоговая стратегия:

```
1. Owner = Gnosis Safe (3 из 5) ✅
2. Пополнять порциями по 100,000 токенов ✅
3. Мониторить баланс ✅
4. При взломе → новый контракт ✅
```

**Вывод:** Ваша стратегия правильная и безопасная! 👍

