# Automatic Airdrop with Transparency

## Ваш случай: Автоматическая отправка + Прозрачность

Вы хотите **сами отправлять токены** получателям (не клейм), но при этом **прозрачно**.

## Решение: Merkle Tree + Batch Send

### Как это работает:

1. **Публикуете CSV** → Все видят список получателей (прозрачность)
2. **Генерируете Merkle Tree** → Создается верифицируемая структура
3. **Устанавливаете Merkle Root** → На контракте (верифицируемо)
4. **Отправляете токены батчами** → Через Safe, но с валидацией Merkle (прозрачно)

## Прозрачность + Автоматическая отправка

### Преимущества:

✅ **Прозрачность** - CSV публикуется, все видят получателей  
✅ **Верифицируемость** - Merkle root на контракте  
✅ **Автоматическая отправка** - Вы отправляете, не пользователи  
✅ **Валидация** - Контракт проверяет, что вы отправляете правильные суммы  

## Процесс:

### Шаг 1: Публикуйте CSV

```bash
# Ваш CSV файл
token-balances.csv
address,amount
0x123...,1000
0x456...,2000
```

**Публикуйте на:**
- GitHub
- IPFS  
- Ваш сайт

**Все видят список получателей!**

### Шаг 2: Генерируйте Merkle Tree

```bash
node scripts/generate-merkle-tree.js token-balances.csv
```

**Результат:**
- `merkle-root.json` - Root для контракта
- `merkle-tree-data.json` - Полные данные

### Шаг 3: Установите Root на контракте

```javascript
// Через Safe (один раз)
await airdrop.setMerkleRoot(merkleRoot);
```

**Теперь любой может проверить:**
- Скачать CSV
- Сгенерировать Merkle tree
- Сравнить root с контрактом
- ✅ Если совпадает → все честно!

### Шаг 4: Генерируйте транзакции для отправки

```bash
node scripts/airdrop-send-automatic.js token-balances.csv
```

**Скрипт создаст:**
- `airdrop-transactions.json` - Данные для Safe
- Батчи по 100 получателей
- С Merkle proofs для валидации

### Шаг 5: Отправляйте через Safe

```javascript
// Через Safe интерфейс
// Используйте данные из airdrop-transactions.json
// batchSend(recipients, amounts, merkleProofs)
```

**Контракт проверит:**
- ✅ Каждый получатель есть в Merkle tree
- ✅ Сумма правильная
- ✅ Не отправлено дважды

## Два варианта отправки:

### Вариант 1: С Merkle валидацией (Рекомендуется)

```solidity
function batchSend(
    address[] calldata recipients,
    uint256[] calldata amounts,
    bytes32[][] calldata merkleProofs
) external onlyOwner
```

**Плюсы:**
- ✅ Контракт проверяет каждую отправку
- ✅ Невозможно отправить неверную сумму
- ✅ Максимальная прозрачность

### Вариант 2: Прямая отправка (Быстрее)

```solidity
function batchSendDirect(
    address[] calldata recipients,
    uint256[] calldata amounts
) external onlyOwner
```

**Плюсы:**
- ✅ Быстрее (нет проверки Merkle)
- ✅ Меньше газа

**Минусы:**
- ⚠️ Нет автоматической валидации
- ⚠️ Можете случайно отправить неверную сумму

## Прозрачность гарантирована:

### Что видят все:

1. **CSV файл** → Все получатели и суммы
2. **Merkle Root** → На контракте (верифицируемо)
3. **Все транзакции** → В блокчейне (прозрачно)
4. **Валидация** → Контракт проверяет каждую отправку

### Пример:

```
📋 Публичная информация:
- CSV: https://github.com/project/airdrop.csv
- Merkle Root: 0xabc123... (на контракте)
- Contract: 0xdef456...

🔍 Проверка:
1. Скачать CSV
2. Сгенерировать Merkle tree
3. Сравнить root с контрактом
4. ✅ Если совпадает → все честно!

📊 Статистика:
- Total Recipients: 10,000
- Total Sent: 8,500 (85%)
- View on: https://explorer.com/airdrop
```

## Сравнение подходов:

| Аспект | Multisig без Merkle | Merkle + Batch Send |
|--------|---------------------|---------------------|
| **Прозрачность** | ⚠️ Только транзакции | ✅ CSV + Root |
| **Верифицируемость** | ⚠️ Сложно проверить | ✅ Легко проверить |
| **Валидация** | ❌ Нет проверки | ✅ Контракт проверяет |
| **Удобство** | ⚠️ Медленно | ⚠️ Медленно (но безопаснее) |

## Итог:

✅ **Вы отправляете токены** (не пользователи клеймят)  
✅ **Прозрачно** (CSV публикуется)  
✅ **Верифицируемо** (Merkle root на контракте)  
✅ **Безопасно** (контракт проверяет каждую отправку)  
✅ **Мультиподпись** (только для отправки, не для каждого клейма)  

Это лучший баланс между удобством и прозрачностью! 🚀

# Airdrop Transparency with Merkle Tree

## Проблема с Multisig для Airdrop

**С мультиподписью:**
- ❌ Неудобно (нужны 3+ подписи на каждый батч)
- ❌ Медленно
- ⚠️ Менее прозрачно (транзакции видны, но список получателей не всегда очевиден)

## Решение: Merkle Tree = Прозрачность + Удобство

### Как это работает:

1. **Вы публикуете CSV** → Все видят список получателей
2. **Генерируете Merkle Tree** → Создается верифицируемая структура
3. **Публикуете Merkle Root** → Любой может проверить, что получатель в списке
4. **Пользователи клеймят сами** → Без мультиподписи, быстро

## Прозрачность Merkle Tree

### 1. Публикация CSV (Прозрачность данных)

```javascript
// Ваш CSV файл можно опубликовать:
// - На GitHub
// - На IPFS
// - На вашем сайте
// - В документации

// Пример: token-balances.csv
address,amount
0x123...,1000
0x456...,2000
0x789...,1500
```

**Любой может:**
- ✅ Увидеть весь список получателей
- ✅ Проверить суммы
- ✅ Убедиться, что он в списке

### 2. Merkle Root на контракте (Верифицируемость)

```solidity
// На контракте хранится только root
bytes32 public merkleRoot; // = 0xabc123...

// Любой может проверить:
// 1. Взять ваш CSV
// 2. Сгенерировать Merkle tree
// 3. Сравнить root с контрактом
// 4. Если совпадает → все честно!
```

### 3. Пользователь может проверить себя

```javascript
// Пользователь проверяет:
// 1. Есть ли его адрес в CSV? ✅
// 2. Правильная ли сумма? ✅
// 3. Может ли он клеймить? ✅

// Если все ОК → клеймит токены
await airdrop.claim(amount, proof);
```

## Полная Прозрачность

### Что видно всем:

1. **CSV файл** → Все получатели и суммы
2. **Merkle Root** → На контракте (верифицируемо)
3. **Все клеймы** → В блокчейне (прозрачно)
4. **Невостребованные токены** → Видно, кто не клеймил

### Пример прозрачности:

```
📋 Публичные данные:
- CSV: https://github.com/yourproject/airdrop.csv
- Merkle Root: 0xabc123... (на контракте)
- Total Recipients: 10,000
- Total Amount: 50,000,000 SAV

🔍 Любой может проверить:
1. Скачать CSV
2. Сгенерировать Merkle tree
3. Сравнить root с контрактом
4. Убедиться, что все честно

✅ Прозрачность: 100%
```

## Сравнение: Multisig vs Merkle Tree

| Аспект | Multisig | Merkle Tree |
|--------|----------|-------------|
| **Прозрачность списка** | ⚠️ Только транзакции | ✅ CSV публикуется |
| **Верифицируемость** | ⚠️ Нужно проверять транзакции | ✅ Root на контракте |
| **Удобство** | ❌ Медленно | ✅ Быстро |
| **Проверка пользователем** | ⚠️ Сложно | ✅ Легко (есть в CSV?) |
| **Публичность** | ⚠️ Ограничена | ✅ Полная |

## Как сделать максимально прозрачно

### Шаг 1: Публикуйте CSV

```bash
# Опубликуйте на GitHub
git add token-balances.csv
git commit -m "Airdrop recipients list"
git push

# Или на IPFS
ipfs add token-balances.csv
# Получите hash: QmXxx...
```

### Шаг 2: Генерируйте Merkle Tree

```bash
node scripts/generate-merkle-tree.js token-balances.csv
```

**Результат:**
- `merkle-root.json` - Root для контракта
- `merkle-tree-data.json` - Полные данные с proofs

### Шаг 3: Публикуйте Merkle Root

```javascript
// Установите root на контракте
await airdrop.setMerkleRoot(merkleRoot);

// Root теперь на контракте - все видят!
// Любой может проверить, что CSV соответствует root
```

### Шаг 4: Публикуйте данные для пользователей

```javascript
// На вашем сайте/API
// Пользователь вводит адрес
const userData = airdropData.recipients.find(r => 
  r.address.toLowerCase() === userAddress.toLowerCase()
);

if (userData) {
  // Показываем:
  // - Сумму для клейма
  // - Кнопку "Claim"
  // - Proof генерируется автоматически
}
```

## Верификация для пользователей

### Пользователь может проверить:

1. **Есть ли я в списке?**
   ```javascript
   // Скачать CSV
   // Найти свой адрес
   // Увидеть сумму
   ```

2. **Правильный ли root?**
   ```javascript
   // Сгенерировать Merkle tree из CSV
   // Сравнить root с контрактом
   // Если совпадает → все честно
   ```

3. **Могу ли я клеймить?**
   ```javascript
   // Проверить на контракте
   const claimed = await airdrop.hasClaimed(userAddress);
   // Если false → можно клеймить
   ```

## Пример: Полная Прозрачность

### Публичная информация:

```
📊 Airdrop Details:
- Total Recipients: 10,000
- Total Amount: 50,000,000 SAV
- CSV File: https://github.com/project/airdrop.csv
- Merkle Root: 0xabc123... (verify on contract)
- Contract: 0xdef456...
- Claim Period: Jan 1 - Feb 1, 2024

🔍 Verification:
1. Download CSV from GitHub
2. Generate Merkle tree: node generate-merkle-tree.js
3. Compare root with contract: airdrop.merkleRoot()
4. If match → ✅ Verified!

📋 Claim Status:
- Claimed: 8,500 (85%)
- Unclaimed: 1,500 (15%)
- View on: https://explorer.com/airdrop
```

## Преимущества для вас:

✅ **Прозрачность** - Все видят список получателей  
✅ **Верифицируемость** - Root на контракте доказывает честность  
✅ **Удобство** - Пользователи клеймят сами  
✅ **Доверие** - Прозрачность = больше доверия  
✅ **Нет мультиподписи** - Только для setup  

## Итог

**С Merkle Tree:**
- ✅ CSV публикуется → все видят получателей
- ✅ Merkle Root на контракте → верифицируемо
- ✅ Пользователи клеймят сами → быстро
- ✅ Полная прозрачность → больше доверия

**Мультиподпись нужна только для:**
- Деплоя контракта (один раз)
- Установки Merkle Root (один раз)
- Вывода невостребованных токенов (после окончания)

**Для пользователей:**
- Нет мультиподписи
- Быстро и удобно
- Прозрачно и верифицируемо

Это стандартный подход для прозрачных airdrop! 🚀

